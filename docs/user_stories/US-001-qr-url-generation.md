# US-001: Генерация QR-кода с URL

## Метаданные
- **ID:** US-001
- **Эпик:** QR Code Generation
- **Приоритет:** P0 - Критичный
- **Оценка:** 5 story points
- **Статус:** ✅ Реализовано
- **Версия:** 0.1.0
- **Создано:** 2025-11-12
- **Обновлено:** 2025-11-12

---

## User Story

**Как** маркетинговый менеджер
**Я хочу** генерировать QR-код, содержащий URL моего сайта
**Чтобы** я мог печатать его на рекламных материалах для клиентов

---

## Бизнес-ценность

### Ценность для бизнеса:
- ⬆️ Увеличение трафика на сайт с офлайн-материалов
- 📊 Возможность отслеживать эффективность кампаний
- 🚀 Снижение барьера для клиентов (не нужно вручную печатать)
- 💰 Оценочная ценность: $10,000/год в дополнительном трафике

### Ценность для пользователя:
- ⏱️ Экономия времени: 2-3 минуты на создание QR
- 🎨 Кастомизация под бренд
- 📱 Использование прямо в Telegram

---

## Acceptance Criteria

### AC1: Базовая генерация QR-кода с URL

```gherkin
Дано я нахожусь на странице генератора
Когда я ввожу "https://mycompany.com/promo" в поле URL
И я нажимаю "Скачать QR"
Тогда PNG-файл должен быть загружен
И сканирование QR-кода должно открывать точный URL
И имя файла должно быть "qr-mycompany.com.png"
```

**Критерии проверки:**
- ✅ QR-код генерируется в формате PNG
- ✅ QR-код содержит корректный URL
- ✅ QR-код сканируется всеми популярными сканерами (iOS Camera, Android Camera, ZXing)
- ✅ Размер файла: 256x256px по умолчанию

---

### AC2: Нормализация URL

```gherkin
Дано я ввожу URL без схемы (например, "example.com")
Когда я генерирую QR-код
Тогда система должна автоматически добавить "https://"
И QR-код должен кодировать "https://example.com"
```

**Правила нормализации:**
- `example.com` → `https://example.com`
- `http://example.com` → `http://example.com` (сохраняется http если указан)
- `https://example.com` → `https://example.com` (без изменений)

**Критерии проверки:**
- ✅ Протокол по умолчанию: `https://`
- ✅ Существующий протокол не перезаписывается
- ✅ Корректная обработка URL с портами: `example.com:8080` → `https://example.com:8080`

---

### AC3: Валидация некорректного URL

```gherkin
Дано я ввожу некорректный URL (например, "not_a_url")
Когда я пытаюсь сгенерировать QR
Тогда должно появиться сообщение об ошибке
И ошибка должна содержать текст "Укажите корректный домен"
И кнопка скачивания должна быть неактивна
```

**Правила валидации:**
- ❌ Отклонять строки без точек: `notadomain`
- ❌ Отклонять пробелы: `my domain.com`
- ❌ Отклонять только специальные символы: `@#$%`
- ✅ Принимать поддомены: `sub.example.com`
- ✅ Принимать пути: `example.com/path/to/page`
- ✅ Принимать параметры: `example.com?param=value`

**Критерии проверки:**
- ✅ Валидация срабатывает в реальном времени
- ✅ Сообщение об ошибке отображается под полем ввода
- ✅ Кнопка "Скачать QR" disabled при невалидном URL

---

### AC4: Превью в реальном времени

```gherkin
Дано я ввожу валидный URL
Когда я изменяю URL
Тогда превью QR-кода должно обновиться
И обновление должно произойти в течение 500ms
И интерфейс не должен зависнуть
```

**Критерии проверки:**
- ✅ Превью обновляется мгновенно
- ✅ Используется debounce для оптимизации
- ✅ Нет блокировки UI во время генерации

---

### AC5: Экспорт в различных размерах

```gherkin
Дано я сгенерировал QR-код с URL "https://test.com"
Когда я выбираю размер экспорта 512px
И я нажимаю "Скачать QR"
Тогда загруженный файл должен быть 512x512 пикселей
И QR-код должен оставаться сканируемым
```

**Поддерживаемые размеры:**
- 256px (мобильные экраны)
- 512px (веб-отображение)
- 1024px (печать A4)
- 2048px (большие плакаты)
- 4096px (билборды)

**Критерии проверки:**
- ✅ Все размеры сканируемы
- ✅ Качество не теряется при увеличении
- ✅ Размер файла разумный (< 500KB для 4096px)

---

## Технические детали

### Реализация
- **Файлы:**
  - `src/lib/qrTypes.ts` - Определение типа URL
  - `src/components/GeneratorNew.tsx` - UI компонент
  - `src/lib/qrConstants.ts` - Константы конфигурации

- **Библиотеки:**
  - `qr-code-styling` - Генерация QR с кастомизацией
  - `qrcode` - Базовая генерация QR

### Валидация
```typescript
// Regex для валидации домена
const DOMAIN_REGEX = /^([a-z0-9]+(-[a-z0-9]+)*\.)+[a-z]{2,}$/i;

function validateUrl(url: string): string | null {
  if (!url) return "Укажите ссылку";

  const normalized = url.startsWith('http') ? url : `https://${url}`;

  try {
    const parsed = new URL(normalized);
    if (!DOMAIN_REGEX.test(parsed.hostname)) {
      return "Укажите корректный домен";
    }
    return null; // Валидация пройдена
  } catch {
    return "Укажите корректный домен";
  }
}
```

### Нормализация
```typescript
function normalizeUrl(url: string): string {
  // Если уже есть протокол, возвращаем как есть
  if (url.startsWith('http://') || url.startsWith('https://')) {
    return url;
  }

  // Иначе добавляем https://
  return `https://${url}`;
}
```

---

## Тестирование

### BDD Feature
- **Файл:** `specs/features/qr_generation.feature`
- **Сценарии:** 3 основных + 1 структура сценария

### Unit Tests
- **Файл:** `tests/qrTypes.test.mjs`
- **Тесты:**
  - URL normalization
  - Domain validation
  - Payload building

### E2E Tests
- **Файл:** `tests/e2e/qr-scannable.spec.ts`
- **Тесты:**
  - QR generation and scannability
  - Real QR decode with jsQR

---

## Out of Scope

**Что НЕ включено в эту story:**

- ❌ Динамические QR-коды (отдельная story)
- ❌ Аналитика сканирований (будущая feature)
- ❌ QR-коды с паролями (отдельная story)
- ❌ Batch URL generation (см. US-002)
- ❌ URL shortening интеграция

---

## Success Metrics

### Функциональные метрики:
- ✅ 100% сценариев BDD проходят
- ✅ 0 критичных багов в production
- ✅ < 500ms время генерации QR

### Бизнес-метрики:
- 📊 30% от всех генераций - URL тип
- 📊 < 2% процент ошибок валидации
- 📊 90%+ пользователей успешно скачивают QR

---

## Dependencies

### Upstream (блокируют эту story):
- Нет (базовая функциональность)

### Downstream (зависят от этой story):
- US-002: Batch QR Generation
- US-005: Dynamic QR Codes
- US-010: URL Analytics

---

## Definition of Done

- [x] Feature file написан (`qr_generation.feature`)
- [x] Step definitions реализованы
- [x] BDD тесты проходят (все сценарии Green)
- [x] Unit тесты проходят
- [x] E2E тесты проходят
- [x] Код прошел code review
- [x] Документация обновлена
- [x] Нет регрессий в существующем функционале
- [ ] Living documentation опубликована (TODO: настроить)
- [x] Задеплоено в production
- [ ] Product Owner принял работу (TODO)

---

## Риски и Mitigation

| Риск | Вероятность | Влияние | Mitigation |
|------|-------------|---------|------------|
| QR не сканируется на некоторых устройствах | Низкая | Высокое | Тестирование на 5+ сканерах, автотесты с jsQR |
| URL слишком длинный для QR | Средняя | Среднее | Предупреждение если > 2000 символов |
| Производительность на слабых устройствах | Низкая | Низкое | Web Worker для генерации, debounce |

---

## История изменений

| Дата | Версия | Изменения | Автор |
|------|--------|-----------|-------|
| 2025-11-12 | 1.0 | Создание user story | Claude |

---

## Приложения

### Примеры использования:

**Use Case 1: Маркетинговая кампания**
```
Компания печатает флаеры с QR-кодом на URL:
https://mycompany.com/summer-sale?utm_source=flyer&utm_campaign=summer2025

Ожидаемый результат:
- QR-код размером 1024px
- Брендовые цвета (#FF0000 на белом фоне)
- Логотип компании в центре
- Сканирование → прямая ссылка на лендинг распродажи
```

**Use Case 2: Визитная карточка**
```
Фрилансер создает QR-код для визитки:
https://portfolio.com/john-doe

Ожидаемый результат:
- Компактный QR (256px)
- Стиль "точки" для элегантности
- Черно-белый дизайн
- Печать на визитке 85x55mm
```

---

**Prepared by:** Claude AI Assistant
**Reviewed by:** [Pending]
**Approved by:** [Pending]
