# Принципы разработки для AI-агентов (Agent Development Guidelines)

**Версия:** 1.0
**Дата:** 2025-11-12
**Проект:** QR Suite

---

## Философия разработки

Этот проект следует **Specification-Based Development (SBD)** подходу с акцентом на:
1. Бизнес-требования и пользовательские сценарии
2. Документация → Тесты → Код (в таком порядке!)
3. Поведенческое тестирование (BDD) с Gherkin/Cucumber

---

## 🎯 CORE PRINCIPLES

### 1. Specification-First Development

**Принцип:** Любая новая функциональность начинается со спецификации, а не с кода.

**Обязательная последовательность:**
```
Спецификация → Тесты (Red) → Код (Green) → Рефакторинг
```

**Что это значит:**

#### ✅ ПРАВИЛЬНО:
```bash
# Шаг 1: Создать спецификацию
git add specs/features/new_feature.feature
git add docs/user_stories/US-XXX-new_feature.md
git commit -m "spec: Add business requirements for [feature]"

# Шаг 2: Написать failing тесты
git add specs/step_definitions/new_feature.steps.ts
git commit -m "test: Add failing BDD tests for [feature]"

# Запуск тестов → ВСЕ RED (это ожидаемо!)
npm run test:bdd

# Шаг 3: Минимальная реализация
git add src/...
git commit -m "feat: Implement [feature]"

# Запуск тестов → ВСЕ GREEN
npm run test:bdd
```

#### ❌ НЕПРАВИЛЬНО:
```bash
# НИКОГДА ТАК НЕ ДЕЛАТЬ!
git add src/newFeature.ts
git commit -m "feat: Add new feature"

# Потом тесты (слишком поздно!)
git add tests/...
git commit -m "test: Add tests"
```

---

### 2. Business-Driven Documentation

**Принцип:** Документация описывает "ЗАЧЕМ" и "ЧТО", а не "КАК".

**Требования к документации:**

#### Каждая фича ДОЛЖНА иметь:
- [ ] **Feature file** (Gherkin): `specs/features/[name].feature`
  - Описание в терминах пользовательской ценности
  - Сценарии в формате Given-When-Then
  - Теги для приоритизации (@critical, @p0, @smoke)

- [ ] **User Story**: `docs/user_stories/US-XXX-[name].md`
  - Формат: "Как [роль] я хочу [цель] чтобы [ценность]"
  - Acceptance Criteria в Given-When-Then
  - Бизнес-метрики успеха
  - Definition of Done

- [ ] **Technical Design** (опционально): `docs/technical/[name].md`
  - Только ПОСЛЕ утверждения бизнес-спецификации
  - Архитектурные решения с обоснованием

#### ✅ ПРАВИЛЬНЫЙ пример документации:
```gherkin
# specs/features/dynamic_qr.feature
Функция: Динамические QR-коды
  Как маркетинговый менеджер
  Я хочу изменять URL после печати QR-кода
  Чтобы экономить $50,000/год на перепечатке материалов
```

#### ❌ НЕПРАВИЛЬНЫЙ пример:
```markdown
# Техническая документация (БЕЗ бизнес-контекста)
## Dynamic QR Implementation
Uses Express.js for URL redirects...
```

---

### 3. Test-Driven Development (TDD) with BDD

**Принцип:** Тесты пишутся ДО кода и отражают бизнес-требования.

#### Иерархия тестов:
1. **BDD тесты (Приоритет #1)** - Бизнес-сценарии
   - Файлы: `specs/features/*.feature`
   - Формат: Gherkin (Given-When-Then)
   - Запуск: `npm run test:bdd`
   - Проверяют: Соответствие бизнес-требованиям

2. **Unit-тесты (Приоритет #2)** - Логика компонентов
   - Файлы: `tests/*.test.mjs`
   - Формат: Node.js test runner / Jest
   - Запуск: `npm test`
   - Проверяют: Корректность функций

3. **E2E тесты (Приоритет #3)** - Интеграция
   - Файлы: `tests/e2e/*.spec.ts`
   - Формат: Playwright
   - Запуск: `npm run test:e2e`
   - Проверяют: Работоспособность системы

#### Red-Green-Refactor цикл:
```
🔴 RED:   Написать failing тест (BDD scenario)
         → Тест падает (функция еще не реализована)

🟢 GREEN: Написать минимальный код
         → Тест проходит

🔵 REFACTOR: Улучшить код
            → Тесты все еще проходят
```

---

### 4. Traceability (Прослеживаемость)

**Принцип:** Любой кусок кода должен быть связан с бизнес-требованием.

#### Обязательная связь:
```
Business Need → Feature File → Step Definitions → Implementation → Tests
```

#### Пример:
```
Бизнес-требование: "Экономия $50K на перепечатке материалов"
         ↓
Feature: specs/features/dynamic_qr.feature
         ↓
User Story: docs/user_stories/US-010-dynamic-qr.md
         ↓
Step Definitions: specs/step_definitions/dynamic_qr.steps.ts
         ↓
Implementation: src/lib/dynamicQr.ts
         ↓
Unit Tests: tests/dynamicQr.test.mjs
```

#### В коде добавлять комментарии:
```typescript
/**
 * Динамическая генерация QR-кодов
 *
 * @feature Dynamic QR Codes
 * @spec specs/features/dynamic_qr.feature
 * @story docs/user_stories/US-010-dynamic-qr.md
 * @business-value $50,000/year savings on reprint costs
 */
export class DynamicQRGenerator { ... }
```

---

### 5. Living Documentation

**Принцип:** Документация автоматически генерируется из тестов и всегда актуальна.

#### Что это значит:
- Feature files (Gherkin) = executable specifications
- При запуске `npm run test:bdd` генерируется HTML-отчет
- Отчет публикуется на GitHub Pages
- Бизнес может читать актуальные требования

#### CI/CD обязательно:
```yaml
# .github/workflows/bdd-tests.yml
- name: Generate Living Documentation
  run: npm run docs:generate

- name: Deploy to GitHub Pages
  uses: peaceiris/actions-gh-pages@v3
  with:
    publish_dir: ./docs/living
```

---

### 6. Behavior over Implementation

**Принцип:** Тесты проверяют ПОВЕДЕНИЕ, а не реализацию.

#### ✅ ПРАВИЛЬНО (проверка поведения):
```gherkin
Сценарий: Пользователь генерирует QR-код
  Дано я ввожу "https://example.com"
  Когда я нажимаю "Скачать QR"
  Тогда QR-код должен быть сканируемым
  И QR-код должен содержать "https://example.com"
```

#### ❌ НЕПРАВИЛЬНО (проверка реализации):
```typescript
test('QRGenerator calls qrcode.generate()', () => {
  const spy = jest.spyOn(qrcode, 'generate');
  generator.generate('https://example.com');
  expect(spy).toHaveBeenCalled();  // ← Проверка реализации!
});
```

---

## 📋 WORKFLOW ДЛЯ AI-АГЕНТОВ

### Когда пользователь просит новую фичу:

#### Этап 1: Discovery & Specification (ОБЯЗАТЕЛЬНО!)

```markdown
1. Уточнить бизнес-контекст:
   - Какую проблему решаем?
   - Кто пользователи?
   - Какая ожидаемая ценность?

2. Создать Feature File:
   - Файл: specs/features/[name].feature
   - Формат: Gherkin на русском
   - Теги: @priority, @feature-name

3. Создать User Story:
   - Файл: docs/user_stories/US-XXX-[name].md
   - Формат: Шаблон из docs/templates/FEATURE_TEMPLATE.md
   - Acceptance Criteria в Given-When-Then

4. Согласовать с пользователем:
   - Показать Feature File
   - Показать User Story
   - Получить подтверждение ПЕРЕД кодированием
```

#### Этап 2: Test Implementation

```markdown
5. Создать Step Definitions:
   - Файл: specs/step_definitions/[name].steps.ts
   - Имплементация Given/When/Then шагов
   - Использовать Page Object Model

6. Запустить тесты → RED:
   - npm run test:bdd
   - Убедиться что все сценарии FAIL (это ожидаемо)
```

#### Этап 3: Implementation

```markdown
7. Написать минимальный код:
   - Только то, что нужно для прохождения тестов
   - YAGNI принцип

8. Запустить тесты → GREEN:
   - npm run test:bdd
   - Все сценарии PASS

9. Рефакторинг (если нужно):
   - Улучшить читаемость
   - Оптимизировать производительность
   - Тесты всё ещё GREEN
```

#### Этап 4: Documentation & Commit

```markdown
10. Обновить документацию:
    - Living documentation (автогенерация)
    - README (если нужно)
    - API docs (если нужно)

11. Коммит в правильном порядке:
    git add specs/features/[name].feature
    git add docs/user_stories/US-XXX-[name].md
    git commit -m "spec: Add business requirements for [feature]"

    git add specs/step_definitions/[name].steps.ts
    git commit -m "test: Add BDD tests for [feature]"

    git add src/...
    git commit -m "feat: Implement [feature]"
```

---

## 🚫 АНТИПАТТЕРНЫ (Что НЕ делать)

### 1. ❌ Code-First Development
```
НЕПРАВИЛЬНО:
Пользователь: "Добавь динамические QR-коды"
Агент: *сразу пишет код*
```

```
ПРАВИЛЬНО:
Пользователь: "Добавь динамические QR-коды"
Агент: "Давайте сначала определим требования:
        - Какую проблему решаем?
        - Кто целевая аудитория?
        - Какие acceptance criteria?
        Я создам Feature File для согласования..."
```

### 2. ❌ Тесты после кода
```
НЕПРАВИЛЬНО:
git commit -m "feat: Add dynamic QR"
git commit -m "test: Add tests for dynamic QR"  ← Слишком поздно!
```

### 3. ❌ Техническая документация вместо бизнес-требований
```
НЕПРАВИЛЬНО:
"Uses Redis for caching, Express for routing..."

ПРАВИЛЬНО:
"Как маркетинговый менеджер я хочу изменять URL после печати
 чтобы экономить $50K/год на перепечатке"
```

### 4. ❌ Тесты проверяют реализацию, а не поведение
```
НЕПРАВИЛЬНО:
expect(mockFunction).toHaveBeenCalled()

ПРАВИЛЬНО:
expect(qrCode).toBeDecodable()
expect(scannedUrl).toBe('https://expected.com')
```

### 5. ❌ Отсутствие связи с бизнес-требованиями
```
НЕПРАВИЛЬНО:
// Функция для генерации QR
function generateQR() { ... }

ПРАВИЛЬНО:
/**
 * @feature Dynamic QR Codes
 * @story US-010: Change URL without reprinting
 * @business-value $50K/year savings
 */
function generateDynamicQR() { ... }
```

---

## 🔧 ИНСТРУМЕНТЫ И КОМАНДЫ

### Обязательные команды для агента:

```bash
# Проверка спецификаций
npm run lint:gherkin          # Валидация Gherkin синтаксиса
npm run spec:coverage         # Покрытие фич спецификациями

# BDD тестирование (ПРИОРИТЕТ!)
npm run test:bdd              # Запуск Cucumber тестов
npm run test:bdd:watch        # Watch режим для разработки

# Дополнительные тесты
npm test                      # Unit тесты
npm run test:e2e              # E2E тесты (Playwright)
npm run test:all              # Все тесты

# Living Documentation
npm run docs:generate         # Генерация HTML из feature files
npm run docs:serve            # Локальный просмотр документации
npm run docs:deploy           # Публикация на GitHub Pages

# CI/CD
npm run ci                    # Полный CI pipeline локально
```

### Структура файлов:

```
specs/
├── features/                 # Gherkin спецификации
│   ├── qr_generation.feature
│   ├── batch_processing.feature
│   └── dynamic_qr.feature
│
├── step_definitions/         # Реализация шагов
│   ├── qr_generation.steps.ts
│   └── common.steps.ts
│
└── support/                  # Вспомогательные утилиты
    ├── hooks.ts
    └── world.ts

docs/
├── user_stories/             # User Stories
│   ├── US-001-qr-url.md
│   └── US-010-dynamic-qr.md
│
├── templates/                # Шаблоны для новых фич
│   └── FEATURE_TEMPLATE.md
│
└── living/                   # Автогенерируемая документация
    └── index.html            # Living Documentation
```

---

## 📊 МЕТРИКИ КАЧЕСТВА

### Обязательные метрики для проверки:

| Метрика | Минимум | Целевое значение |
|---------|---------|------------------|
| **Spec Coverage** | 70% | 90% |
| **BDD Tests Pass Rate** | 100% | 100% |
| **Feature → Test → Code Traceability** | 80% | 100% |
| **Living Documentation Актуальность** | 80% | 100% |
| **Commits следуют Spec-First** | 70% | 95% |

### Проверка качества процесса:

```bash
# Каждый PR должен проходить:
1. Gherkin lint (без ошибок)
2. BDD тесты (100% pass)
3. Unit тесты (>80% coverage)
4. E2E тесты (pass)
5. Living documentation сгенерирована
```

---

## 🎓 ОБУЧАЮЩИЕ МАТЕРИАЛЫ

### Для команды:

1. **BDD и Gherkin:**
   - [Cucumber Documentation](https://cucumber.io/docs/)
   - [BDD in Action by John Ferguson Smart](https://www.manning.com/books/bdd-in-action)

2. **Specification by Example:**
   - [Gojko Adzic - Specification by Example](https://gojko.net/books/specification-by-example/)
   - [Example Mapping](https://cucumber.io/blog/bdd/example-mapping-introduction/)

3. **Test-Driven Development:**
   - [Kent Beck - Test Driven Development](https://www.amazon.com/Test-Driven-Development-Kent-Beck/dp/0321146530)

---

## 📝 CHECKLIST ДЛЯ AI-АГЕНТА

Перед началом работы над фичей:

- [ ] Я уточнил бизнес-контекст и ценность?
- [ ] Я создал Feature File с Gherkin сценариями?
- [ ] Я создал User Story с Acceptance Criteria?
- [ ] Я согласовал спецификацию с пользователем?
- [ ] Я написал Step Definitions и они FAIL?
- [ ] Только после этого я начинаю писать код?

Перед коммитом:

- [ ] BDD тесты проходят (GREEN)?
- [ ] Unit тесты проходят?
- [ ] Код связан со спецификацией (traceability)?
- [ ] Living documentation обновлена?
- [ ] Коммиты в правильном порядке (spec → test → code)?

---

## 🤝 THREE AMIGOS PROCESS

Для критичных фич (P0, P1) рекомендуется процесс Three Amigos:

### Участники:
- **Product Owner** (бизнес-требования)
- **Developer** (техническая реализация)
- **QA Engineer** (тестирование, edge cases)

### Процесс:
1. **Презентация проблемы** (10 мин)
2. **Example Mapping** (20 мин)
   - Определение правил
   - Примеры использования
   - Вопросы и неизвестные
3. **Написание Gherkin** (30 мин)
4. **Оценка и согласование DoD** (10 мин)

### Выход:
- `.feature` файл с согласованными сценариями
- User Story с acceptance criteria
- Оценка трудозатрат

---

## 🔄 CONTINUOUS IMPROVEMENT

### Ретроспектива процесса (каждый спринт):

1. Что работает хорошо?
2. Что можно улучшить?
3. Соблюдаем ли мы Spec-First принцип?
4. Актуальна ли Living Documentation?
5. Есть ли фичи без спецификаций?

### Action Items:
- Обновить шаблоны
- Улучшить автоматизацию
- Обучить команду

---

## 📞 ВОПРОСЫ И ПОДДЕРЖКА

Если возникают вопросы о процессе:
1. Прочитать этот документ
2. Посмотреть примеры в `specs/features/`
3. Использовать шаблоны из `docs/templates/`
4. Спросить у команды

---

**Главный принцип:**
> "Specification-First Development - это не бюрократия, а инструмент для создания правильного продукта. Если мы не знаем ЗАЧЕМ мы пишем код, мы не должны его писать."

---

**Версия:** 1.0
**Последнее обновление:** 2025-11-12
**Авторы:** QR Suite Team + Claude AI
**Статус:** ✅ Утверждено для использования
